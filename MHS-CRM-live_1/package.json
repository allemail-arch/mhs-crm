{
  "name": "mhs-crm",
  "version": "1.0.0",
  "description": "MHS CRM — full app (frontend + API). Core is Node builtins; WhatsApp capture uses Baileys.",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server/server.js",
    "seed": "node server/seed.js reset"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.0",
    "qrcode": "^1.5.4"
  }
}
